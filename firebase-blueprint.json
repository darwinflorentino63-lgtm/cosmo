{
  "entities": {
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "A message in a chat session",
      "type": "object",
      "properties": {
        "role": {
          "type": "string",
          "enum": ["user", "bot"],
          "description": "The role of the message sender"
        },
        "content": {
          "type": "string",
          "description": "The content of the message"
        },
        "sessionId": {
          "type": "string",
          "description": "The ID of the chat session"
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp of when the message was created"
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this chat"
        }
      },
      "required": ["role", "content", "sessionId", "createdAt", "userId"]
    }
  },
  "firestore": {
    "/users/{userId}/chats/{chatId}": {
      "schema": {
        "$ref": "#/entities/ChatMessage"
      },
      "description": "Chat messages for a specific user"
    }
  }
}
